# PuttingCaddy 단일 앱 안내

현재 워크스페이스는 `PuttingCaddy` 단일 앱 기준으로 사용합니다.

## 앱 식별자

- 런처 이름: `퍼팅캐디`
- `applicationId`: `com.wmcho.puttingcaddy`

## 작업 경로

- 권장 폴더: `StudioProjects/PuttingCaddy`
- Flutter/Android 작업은 이 저장소 루트에서 진행합니다.

## 빌드 예시

```bash
flutter pub get
flutter build appbundle
```


